# Datenmengen, Scan & Übertragung — komplett rechnen

<MetaBar
  lernfeld="LF 2 — Datenmengen, Scan und Übertragung"
  prüfungsrelevanz="Grundlagenrechnen · D3"
/>

Dieses Kapitel bündelt die Rechnungen für Datenmengen: Bit/Byte und Präfixe als
Fundament, Scan- und Bilddaten als Speicherbedarf sowie Übertragungsdauer mit einer
vorgegebenen effektiven Datenrate. Danach kannst du Dateigrößen, Archivbedarf und
einfache Backup-Zeiten mit einem einheitlichen Verfahren bestimmen.

Für eine ausführliche Einordnung von Bandbreite, Durchsatz und realen Downloadzeiten
nutze anschließend das Kapitel
<a href="/lernen/netzwerke/rechnen-netz/datenrate-berechnung">Datenrate &amp; Downloadzeit berechnen</a>.

<Tipp titel="So benutzt du dieses Kapitel">
Lies zuerst **Grundprinzip** und **Mechanik**. Danach reichen oft die drei Beispiele:
Scan-Speicher, Backup-Übertragung und Video-Bitrate. Der Abschnitt zu Zusammenhängen ist
Nachschlagewerk, wenn du eine Einheit oder einen Begriff sauber einordnen willst.
</Tipp>

| Wenn der Fall fragt nach ... | Denk zuerst an ... | Typische Falle |
|---|---|---|
| Speicher für Bilder, Scans, Video | Pixel × Farbtiefe oder Bitrate × Dauer | Bit mit Byte verwechseln |
| Download-, Backup- oder Sync-Zeit | Datenmenge ÷ Datenrate | Mbit/s nicht in MB/s umrechnen |
| KB/MB/GB gegen KiB/MiB/GiB | 1000er- gegen 1024er-Potenzen | Dezimal und binär mischen |

## 01 · Warum existiert das überhaupt?

Im Alltag musst du schnell abschätzen können, wie viel Speicher ein Jahr Belegscan
kostet, wie lange eine Sicherung bei einer vorgegebenen Nettorate dauert und ob ein
Video mit einer gegebenen Bitrate auf einen Datenträger passt. Diese Fälle greifen auf
dieselben Bausteine zu: *<Term id="bit">Bit</Term>* und
*<Term id="byte">Byte</Term>*, saubere Präfixe und eine passende Formel. Wer die
Einheiten zuerst klärt, erkennt den häufigsten Fehler sofort: Eine 24-Bit-Farbtiefe
bedeutet 3 Byte pro Pixel, nicht 24 Byte.

---

## 02 · Das Grundprinzip

### Bit und Byte — die zwei kleinsten Einheiten

Die Grundlagen zu Bit und Byte setzt dieses Kapitel voraus. Entscheidend für jede
Rechnung ist: Acht Bit ergeben ein Byte. Schreibe die Einheit deshalb bei jedem
Zwischenergebnis auf.

```
1 Byte = 8 Bit
1 KiB  = 1024 Byte           (binär, IEC 80000-13)
1 KB   = 1000 Byte           (dezimal, SI)
```

Daraus folgt der wichtigste Faktor jeder Übertragungsaufgabe: **8**.

```
MB/s   = Mbit/s ÷ 8
Mbit/s = MB/s × 8
```

Die Eselsbrücke: kleines `b` ist das kleine Bit, großes `B` das große Byte. Vor einer
Division oder Multiplikation müssen Datenmenge und Datenrate in dieselbe Einheit
gebracht werden.

### SI-Präfixe (dezimal) gegen IEC-Präfixe (binär)

Zwei Welten teilen sich dieselben Buchstaben — und genau das ist die häufigste Falle:

| Präfix-Typ | Zeichen | Wert | Quelle |
|---|---|---|---|
| *<Term id="dezimalpraefix">Dezimal</Term>* | KB, MB, GB, TB | 10³, 10⁶, 10⁹, 10¹² Byte | SI-Brochure (BIPM) |
| *<Term id="binaerpraefix">Binär</Term>* | KiB, MiB, GiB, TiB | 2¹⁰, 2²⁰, 2³⁰, 2⁴⁰ Byte | IEC 80000-13 / NIST |

```
1 KB  = 1.000 Byte
1 KiB = 1.024 Byte           (~ 2,4 Prozent größer)
1 MB  = 1.000.000 Byte
1 MiB = 1.048.576 Byte       (~ 4,9 Prozent größer)
1 GB  = 1.000.000.000 Byte
1 GiB = 1.073.741.824 Byte   (~ 7,4 Prozent größer)
1 TB  = 10¹² Byte
1 TiB = 2⁴⁰ Byte             (~ 10,0 Prozent größer)
```

Welche Reihe gilt, steht in der Aufgabe: Bei `MB` und `GB` rechnest du mit
1000er-Potenzen, bei `MiB` und `GiB` mit 1024er-Potenzen. Mische die Reihen nicht.

### Speicherbedarf — die zwei Grundformeln

**Pro Datei:**

```
Datenmenge = Pixelanzahl × Farbtiefe ÷ 8
oder
Datenmenge = Bitrate × Dauer ÷ 8
```

**Pro Fall:**

```
Gesamtspeicher = Datenmenge je Datei × Anzahl Dateien
```

### Übertragungsdauer — eine einzige Formel

```
Zeit = Datenmenge ÷ effektive Datenrate
```

Beide Größen müssen in derselben Einheit sein — also entweder beides in Bit oder beides in Byte.

### Effektive Datenrate nur mit Aufgabewert

Gibt die Aufgabe einen Wirkungsgrad oder eine Nettorate vor, rechnest du damit. Einen
Prozentwert für echte Netze darfst du nicht aus einer Fausttabelle erfinden: Er hängt
vom konkreten Übertragungsweg und der Messsituation ab.

<Tipp titel="Merksatz">
Datenmenge in Byte, Datenrate in Bit/s — oder umgekehrt? Beides geht, aber nicht in
derselben Rechnung. Erst beides in eine gemeinsame Basis (alles Byte oder alles Bit),
dann teilen oder multiplizieren. Eine Einheit hinter jedem Zwischenergebnis ist Pflicht.
</Tipp>

---

## 03 · Die Mechanik — vom Pixel bis zur Sekunde

### 03.1 · Scan- und Bilddaten — die Vier-Schritt-Formel

Die zentrale Formel verbindet drei Größen in fester Reihenfolge: zuerst die Auflösung in
*<Term id="dpi">DPI</Term>*, dann die *<Term id="farbtiefe">Farbtiefe</Term>* in Bit pro
Pixel, am Ende ein optionaler Faktor für *<Term id="kompression">Kompression</Term>*.

<Schritte
  items={[
    { titel: 'Maße in Pixel umrechnen', text: 'Wenn das Format in Zentimetern angegeben ist (z. B. DIN A4 = 21,0 cm × 29,7 cm), zuerst durch 2,54 teilen für Inch. Dann jede Inch-Zahl mit der DPI-Zahl multiplizieren — getrennt für Breite und Höhe.' },
    { titel: 'Pixelanzahl berechnen', text: 'Pixelanzahl = Breite_px × Höhe_px. Bei einem A4-Scan mit 600 dpi sind das schon 41,7 Megapixel — also schnell sehr große Zahlen.' },
    { titel: 'Bits aus Pixeln und Farbtiefe', text: 'Bits gesamt = Pixelanzahl × Farbtiefe. Die Farbtiefe ist immer in Bit pro Pixel angegeben: 1 Bit S/W, 8 Bit Graustufen, 24 Bit RGB-Farbe, 32 Bit RGB plus Alpha.' },
    { titel: 'Bytes und Zieleinheit', text: 'Bytes = Bits ÷ 8. Danach in die geforderte Einheit umrechnen: KiB (÷ 1024), MiB (÷ 1024²), GiB (÷ 1024³), TiB (÷ 1024⁴). Wenn der Text MB statt MiB nennt, durch 1000-Potenzen teilen.' },
    { titel: 'Kompressions-Faktor anwenden', text: 'Bei „auf 50 Prozent komprimiert" multiplizierst du das Roh-Ergebnis mit 0,5. „Auf 30 Prozent" heißt × 0,3 — also 70 Prozent werden weggespart, 30 Prozent bleiben übrig.' },
    { titel: 'Hochrechnen auf den Zeitraum', text: 'Bei Mengenrechnungen (Scans pro Tag × Arbeitstage) am Ende mit der Stückzahl multiplizieren. Erst zum Schluss runden — Zwischenrundungen verschieben das Ergebnis unnötig.' },
  ]}
/>

### 03.2 · Datenrate, Datenmenge und Übertragungsdauer

Drei Formeln, die du im Schlaf können musst:

```
Datenrate    = Datenmenge ÷ Zeit
Zeit         = Datenmenge ÷ Datenrate
Datenmenge   = Datenrate × Zeit
```

Vor jeder Rechnung: Einheiten vereinheitlichen. Wer Datenmenge in MB und die
*<Term id="datenrate">Datenrate</Term>* in Mbit/s vermischt, rechnet Faktor 8 falsch — der
häufigste Rechenfehler überhaupt.

```
Mbit/s ÷ 8 = MB/s
MB/s   × 8 = Mbit/s
```

Wenn ein Wirkungsgrad in der Aufgabe vorgegeben ist:

```
effektive Datenrate = Bandbreite × Wirkungsgrad
Zeit                = Datenmenge ÷ effektive Datenrate
```

### 03.3 · Rohdaten und vorgegebene Video-Bitrate

Für unkomprimierte Bildfolgen ergibt sich die Roh-Bitrate aus Pixelanzahl je Frame,
Farbtiefe und Frames pro Sekunde. Bei einer komprimierten Datei rechnest du mit der
in der Aufgabe vorgegebenen Bitrate:

```
Roh-Bitrate (Video) = Pixel × Farbtiefe × FPS
Roh-Bitrate (Audio) = Sample-Rate × Bittiefe × Kanäle

Datenmenge je Sekunde = Roh-Bitrate ÷ 8
Datenmenge je Stunde  = Datenmenge je Sekunde × 3600
```

Die Rechnung erklärt ausschließlich die Datenmenge für die genannten Eingabewerte. Sie
trifft keine Aussage darüber, welche Bitrate ein bestimmter Codec in einem anderen Fall
erreicht.

<Tipp titel="Reihenfolge merken">
Pixel → Bits → Byte → Zieleinheit → Stück. Wer in dieser Reihenfolge bleibt, kommt sicher
durch jede Scan-Rechnung. Bei Übertragung gilt: alles in dieselbe Welt (Bit oder Byte),
dann teilen.
</Tipp>

---

## 04 · Konkrete Anwendungsfälle

Die drei Beispiele decken die wichtigsten Rechenformen ab. Du musst sie nicht
auswendig lernen, sondern den Ablauf erkennen: **Einheiten klären, Grundformel wählen,
Netto-Faktor beachten, erst am Ende runden**.

<Beispiel titel="A4-Scan bei 600 dpi — Dateigröße und Speicher für 200 Scans">

Eine Anwaltskanzlei scannt eingehende Verträge mit einem Multifunktionsgerät bei
**600 dpi** und **24 Bit Farbtiefe** in DIN A4 (**21,0 cm × 29,7 cm**). Die Scans werden
**unkomprimiert** im TIFF-Format archiviert. Wie groß ist eine einzelne Datei in MiB,
und wie viel Speicher belegen 200 Scans pro Tag in einer Woche (5 Arbeitstage)?

</Beispiel>

<StepByStep titel="Speicherbedarf für hochauflösende A4-Scans">
  <Schritt nr={1} titel="Format in Inch umrechnen">
    Breite: 21,0 cm ÷ 2,54 ≈ **8,2677 Inch**.
    Höhe: 29,7 cm ÷ 2,54 ≈ **11,6929 Inch**.
    Inch ist die Einheit, auf die sich DPI bezieht. Ohne diesen Schritt wären die
    Pixelzahlen falsch um Faktor 2,54.
  </Schritt>
  <Schritt nr={2} titel="Pixelanzahl bestimmen">
    Breite_px = 8,2677 × 600 ≈ **4961 px**.
    Höhe_px  = 11,6929 × 600 ≈ **7016 px**.
    Pixelanzahl = 4961 × 7016 = **34 806 376 px**.
  </Schritt>
  <Schritt nr={3} titel="Bits und Bytes je Datei">
    Bits = 34 806 376 × **24 Bit** = **835 353 024 Bit**.
    Bytes = 835 353 024 ÷ 8 = **104 419 128 Byte**.
    Die Farbtiefe steht in Bit pro Pixel — wer hier 24 Byte rechnet, hat eine Datei, die
    achtmal zu groß ist.
  </Schritt>
  <Schritt nr={4} titel="In MiB umrechnen">
    MiB = 104 419 128 ÷ 1 048 576 ≈ **99,58 MiB** je Scan.
    Eine einzelne Seite belegt bei diesen Eingabewerten rund 100 MiB.
  </Schritt>
  <Schritt nr={5} titel="Wochenmenge hochrechnen">
    Scans/Woche = 200 × 5 = **1000 Scans**.
    Gesamtbytes = 1000 × 104 419 128 = **104 419 128 000 Byte**.
    GiB = 104 419 128 000 ÷ 1 073 741 824 ≈ **97,25 GiB** für die ganze Woche.
  </Schritt>
  <Ergebnis>
    Eine A4-Seite bei 600 dpi und 24 Bit belegt rund **100 MiB unkomprimiert**. Bei 200
    Scans pro Tag fallen pro Woche knapp **97 GiB** an — pro Jahr (250 Arbeitstage) wären
    das **rund 4,75 TiB**. Eine tatsächliche Archivgröße hängt zusätzlich vom verwendeten
    Dateiformat und den konkret gewählten Einstellungen ab.
  </Ergebnis>
</StepByStep>

<Beispiel titel="8-GB-Backup über VDSL gegen LAN — Brutto und Netto">

Ein 8-GB-Backup-Image soll wahlweise über die VDSL-Leitung des Standorts (**Bandbreite
50 Mbit/s**, Wirkungsgrad annahmegemäß **80 Prozent**) oder über einen 100-Mbit/s-LAN-
Anschluss zu einem internen NAS (**Wirkungsgrad 95 Prozent**) übertragen werden. Wie
lange dauert die Übertragung in jedem Fall, in Sekunden und in Minuten?

</Beispiel>

<StepByStep titel="Übertragungsdauer im Vergleich VDSL gegen LAN">
  <Schritt nr={1} titel="Datenmenge in eine konsistente Einheit">
    8 GB sind dezimal gemeint (Backup-Tools rechnen so): 8 × 10⁹ Byte = **8 000 000 000 Byte**.
    In Bit: 8 000 000 000 × 8 = **64 000 000 000 Bit**, also **64 Gbit**.
  </Schritt>
  <Schritt nr={2} titel="Effektive Datenraten ausrechnen">
    VDSL: 50 Mbit/s × 0,80 = **40 Mbit/s netto**.
    LAN:  100 Mbit/s × 0,95 = **95 Mbit/s netto**.
    Der Wirkungsgrad ist vorgegeben — also einfach multiplizieren, kein Schätzen nötig.
  </Schritt>
  <Schritt nr={3} titel="VDSL — Zeit berechnen">
    Zeit = 64 000 Mbit ÷ 40 Mbit/s = **1600 Sekunden**.
    1600 s ÷ 60 ≈ **26 Minuten 40 Sekunden**.
    Ob dieses Zeitfenster ausreicht, ist eine betriebliche Anforderung und nicht Teil der
    Rechenregel.
  </Schritt>
  <Schritt nr={4} titel="LAN — Zeit berechnen">
    Zeit = 64 000 Mbit ÷ 95 Mbit/s ≈ **673,7 Sekunden**.
    673,7 s ÷ 60 ≈ **11 Minuten 14 Sekunden**.
    Die Rechnung vergleicht nur die beiden vorgegebenen Datenraten.
  </Schritt>
  <Schritt nr={5} titel="Konsistenz-Check">
    Wenn beide Leitungen 100 Prozent Wirkungsgrad hätten: VDSL = 1280 s, LAN = 640 s. Mit
    Wirkungsgrad werden beide Werte größer — VDSL um Faktor 1/0,8 = 1,25, LAN um 1/0,95
    ≈ 1,053. Genau das sehen wir oben (1280 × 1,25 = 1600; 640 × 1,053 ≈ 673,7).
  </Schritt>
  <Ergebnis>
    VDSL: **rund 27 Minuten**, LAN: **rund 11 Minuten**. Die höhere vorgegebene
    Nettodatenrate führt zur kürzeren Zeit. Wer Brutto- und Netto-Werte verwechselt,
    landet bei 1280 s und verfehlt damit die Aufgabenannahme.
  </Ergebnis>
</StepByStep>

<Beispiel titel="Videoaufnahme — Roh-Bitrate gegen H.264">

Ein 4K-Sicherheitskamerasystem soll FullHD-Daten archivieren: **Auflösung 1920 × 1080**,
**Farbtiefe 24 Bit**, **30 Frames pro Sekunde**, **eine Stunde Daueraufnahme**. Wie groß
ist die Aufnahme **unkomprimiert** in GiB? Und wie viel bleibt nach Kompression mit
**H.264 bei 8 Mbit/s**?

</Beispiel>

<StepByStep titel="Speicher und Bitrate für eine Stunde FullHD">
  <Schritt nr={1} titel="Pixel je Frame und Roh-Bitrate">
    Pixel je Frame = 1920 × 1080 = **2 073 600 px**.
    Bits je Frame = 2 073 600 × 24 = **49 766 400 Bit**.
    Roh-Bitrate = 49 766 400 × 30 = **1 492 992 000 Bit/s** ≈ **1,49 Gbit/s**.
  </Schritt>
  <Schritt nr={2} titel="Bytes pro Sekunde und Stunde — unkomprimiert">
    Byte/s = 1 492 992 000 ÷ 8 = **186 624 000 Byte/s** ≈ **177,98 MiB/s**.
    Byte/h = 186 624 000 × 3600 = **671 846 400 000 Byte**.
    GiB/h = 671 846 400 000 ÷ 1 073 741 824 ≈ **625,71 GiB**.
  </Schritt>
  <Schritt nr={3} titel="H.264 — die komprimierte Variante">
    Vorgegeben sind 8 Mbit/s. Byte/s = 8 000 000 ÷ 8 = **1 000 000 Byte/s** = **1 MB/s**.
    Byte/h = 1 000 000 × 3600 = **3 600 000 000 Byte** = **3,6 GB**.
    Damit liegt der komprimierte Wert bei rund **3,35 GiB** für eine Stunde.
  </Schritt>
  <Schritt nr={4} titel="Kompressions-Verhältnis">
    Verhältnis = 671 846 400 000 ÷ 3 600 000 000 ≈ **186,6 : 1**.
    Für diese Eingabewerte beträgt das Verhältnis von Rohdaten zu Ziel-Bitrate rund
    **186,6 : 1**.
  </Schritt>
  <Schritt nr={5} titel="Speicher-Hochrechnung für 30 Tage Daueraufnahme">
    Unkomprimiert: 625,71 GiB × 24 × 30 ≈ **450 508 GiB** ≈ **439,95 TiB** — praktisch
    unbezahlbar.
    H.264: 3,35 GiB × 24 × 30 ≈ **2412 GiB** ≈ **2,36 TiB** — eine Sache für eine handelsübliche
    Festplattenanlage mit RAID 5.
  </Schritt>
  <Ergebnis>
    Roh-Daten würden bei diesen Bildwerten **rund 626 GiB** für eine Stunde FullHD
    verbrauchen. Mit der vorgegebenen Ziel-Bitrate von 8 Mbit/s sind es **etwa 3,35 GiB**
    — Faktor **~ 187** weniger Datenmenge.
  </Ergebnis>
</StepByStep>

---

## 05 · Zusammenhänge — was darüber hinaus wichtig ist

### KB gegen KiB, MB gegen MiB, GB gegen GiB — der ewige Streit

Der Unterschied wirkt klein und kostet im Lauf der Skala viel:

| Größe | dezimal | binär | Differenz |
|---|---|---|---|
| 1 K | 1 000 | 1 024 | + 2,4 % |
| 1 M | 1 000 000 | 1 048 576 | + 4,9 % |
| 1 G | 10⁹ | 2³⁰ | + 7,4 % |
| 1 T | 10¹² | 2⁴⁰ | + 10,0 % |

Bei 1 TiB liegen also schon **rund 100 GB Differenz** zur dezimalen Form. Deshalb wirkt
die neue 1-TB-Festplatte unter Windows als „nur 931 GiB" — der Datenträger hat dezimal
genau 1 000 000 000 000 Byte, das Betriebssystem zeigt aber binär an. Kein Defekt,
sondern Konvention.

---

## 06 · Typische Fehler

- **Bit gegen Byte vertauscht.** Ein kleines `b` ist Bit, ein großes `B` ist Byte. 100 Mbit/s
  sind 12,5 MB/s, nicht 100 MB/s. Wer das einmal vermischt, rechnet jede Folgeaufgabe um
  Faktor 8 falsch.
- **Mbit/s und MB/s verwechselt.** Zur Sicherheit immer aufschreiben: „Datenrate in
  Mbit/s" oder „Datenrate in MB/s". Erst danach rechnen.
- **Vorgegebenen Netto-Faktor ignoriert.** Wenn die Aufgabe einen Wirkungsgrad nennt,
  gehört er zwingend in die Rechnung.
- **Dezimal- mit Binärpräfix vermischt.** 10¹² Byte entsprechen rechnerisch rund
  931 GiB. Bei MiB/GiB konsequent durch 1024 teilen, bei MB/GB durch 1000.
- **Farbtiefe in Byte statt Bit gerechnet.** „24 Bit Farbtiefe" sind 3 Byte pro Pixel, nicht
  24 Byte. Erst Pixel × Farbtiefe (in Bit), dann ÷ 8 für Byte.
- **DPI in nur einer Richtung berücksichtigt.** 600 dpi heißen 600 Punkte je Inch in beide
  Richtungen. Wer nur die Breite multipliziert, hat ein Bild, das in der Höhe falsch ist.
- **cm direkt mit DPI multipliziert.** DPI bezieht sich auf Inch, nicht auf cm. Erst durch
  2,54 teilen, dann mit DPI multiplizieren.
- **„Auf 50 Prozent komprimiert" als Subtraktion gerechnet.** Das heißt × 0,5, nicht
  „minus 50 Byte" und auch nicht „÷ 50". Komprimierung ist immer ein Faktor.
- **Zwischenrundungen.** Erst am Ende auf zwei Nachkommastellen runden; sonst verschiebt
  sich das Ergebnis bei großen Stückzahlen unnötig.

---

## 07 · Mini-Selbstcheck mit Kurzlösungen

Diese Checks sind nur Plausibilitätsanker. Sie zeigen, ob du die drei Umrechnungen
auseinanderhältst.

| Check | Kurzlösung |
|---|---|
| 2 048 000 Bit in KiB und KB | 2 048 000 ÷ 8 = 256 000 Byte; ÷ 1024 = **250 KiB**; ÷ 1000 = **256 KB** |
| 200 Mbit/s mit 85 Prozent Wirkungsgrad in MB/s | 200 × 0,85 = 170 Mbit/s netto; ÷ 8 = **21,25 MB/s** |
| A4 bei 300 dpi und 24 Bit, unkomprimiert | 21,0 ÷ 2,54 × 300 ≈ 2480 px; 29,7 ÷ 2,54 × 300 ≈ 3508 px; × 24 ÷ 8 ≈ **26,10 MB** bzw. **24,89 MiB** |
| 120 GB über 250 Mbit/s mit 75 Prozent Wirkungsgrad | 120 GB = 960 Gbit; netto 187,5 Mbit/s; Zeit = 960 000 ÷ 187,5 = **5120 s**, also **85 min 20 s** |
| FullHD, 30 fps, 24 Bit roh | 1920 × 1080 × 24 × 30 ≈ **1,49 Gbit/s** |

---

## 08 · Merkbilder

Diese Skizzen sind nicht als zusätzlicher Lernstoff gedacht. Sie fassen nur die
beiden Denkmodelle zusammen, die du beim Rechnen immer wieder brauchst:
Einheitenleiter und Brutto-Netto-Verbindung.

<svg
  viewBox="0 0 760 360"
  role="img"
  aria-label="Skizze: Datenmengen-Pyramide mit Bit, Byte, KiB, MiB, GiB, TiB als Stufenturm; Akzent rot auf der Spitze TiB; rechts die Faktoren 8 und 1024."
  className="my-8 w-full max-w-3xl border border-rule bg-paper"
  xmlns="http://www.w3.org/2000/svg"
>
  <defs>
    <style>{`
      .lbl { font: 500 13px 'IBM Plex Sans', sans-serif; fill: #1a2332; }
      .sub { font: 400 11px 'IBM Plex Sans', sans-serif; fill: #4a5568; }
      .hdr { font: 600 13px 'IBM Plex Sans', sans-serif; fill: #1a2332; letter-spacing: 0.04em; }
      .accent { stroke: #8b2f1d; stroke-width: 2; fill: none; }
      .stage { fill: none; stroke: #1a2332; stroke-width: 1.2; }
      .stage-top { fill: none; stroke: #8b2f1d; stroke-width: 2; }
      .tag { font: 500 10px 'JetBrains Mono', monospace; fill: #8b2f1d; }
      .rule { stroke: #1a2332; stroke-width: 1; fill: none; }
    `}</style>
  </defs>

  <text className="hdr" x="20" y="28">Datenmengen-Pyramide · vom Bit zum TiB</text>

  <rect className="stage" x="40" y="290" width="500" height="40" />
  <text className="lbl" x="56" y="316">Bit</text>
  <text className="sub" x="100" y="316">Ausgangseinheit der Rechnung</text>

  <rect className="stage" x="60" y="240" width="460" height="40" />
  <text className="lbl" x="76" y="266">Byte</text>
  <text className="sub" x="120" y="266">8 Bit</text>

  <rect className="stage" x="90" y="190" width="400" height="40" />
  <text className="lbl" x="106" y="216">KiB</text>
  <text className="sub" x="150" y="216">1024 Byte · IEC 80000-13</text>

  <rect className="stage" x="130" y="140" width="320" height="40" />
  <text className="lbl" x="146" y="166">MiB</text>
  <text className="sub" x="190" y="166">1 048 576 Byte (2^20)</text>

  <rect className="stage" x="170" y="90" width="240" height="40" />
  <text className="lbl" x="186" y="116">GiB</text>
  <text className="sub" x="230" y="116">~ 1,07 Mrd. Byte (2^30)</text>

  <rect className="stage-top" x="220" y="40" width="140" height="40" />
  <text className="lbl" x="236" y="66">TiB</text>
  <text className="sub" x="276" y="66">2^40 Byte</text>

  <line className="rule" x1="560" y1="60" x2="560" y2="320" />
  <text className="tag" x="572" y="68">x 1024</text>
  <text className="tag" x="572" y="116">x 1024</text>
  <text className="tag" x="572" y="166">x 1024</text>
  <text className="tag" x="572" y="216">x 1024</text>
  <text className="tag" x="572" y="266">x 8 (Bit -&gt; Byte)</text>
  <text className="sub" x="572" y="316">Faktor zwischen Stufen</text>
</svg>

<Glossar
  einträge={[
    {
      term: 'Bit (b)',
      definition: 'Die in diesem Kapitel verwendete kleinste Einheit. Acht Bit ergeben ein Byte.'
    },
    {
      term: 'Byte (B)',
      definition: 'Acht Bit zusammen; die Rechenbasis für Dateigrößen in diesem Kapitel.'
    },
    {
      term: 'Datenrate',
      definition: 'Datenmenge pro Zeit. Für die Zeitrechnung müssen Datenmenge und Datenrate dieselbe Einheit verwenden.'
    },
    {
      term: 'DPI',
      definition: 'Punkte je Inch. In einer Scanaufgabe rechnest du die Maße deshalb zuerst von Zentimeter in Inch um.'
    },
    {
      term: 'Farbtiefe',
      definition: 'In der Aufgabe gegebene Bitzahl je Pixel. Multipliziere sie mit der Pixelanzahl und teile anschließend durch acht.'
    },
    {
      term: 'Bitrate',
      definition: 'Datenrate eines Medienstroms. Eine gegebene Bitrate multiplizierst du mit der Dauer, um die Datenmenge zu berechnen.'
    },
    {
      term: 'Kompression',
      definition: 'Vorgegebener Faktor für eine Rechenaufgabe. „Auf 50 Prozent" bedeutet, das Roh-Ergebnis mit 0,5 zu multiplizieren.'
    },
    {
      term: 'Binärpräfix (KiB, MiB, GiB, TiB)',
      definition: 'Binäre Vielfache: KiB, MiB, GiB und TiB bauen jeweils auf dem Faktor 1024 auf.'
    },
    {
      term: 'Dezimalpräfix (KB, MB, GB, TB)',
      definition: 'Dezimale Vielfache: KB, MB, GB und TB bauen jeweils auf dem Faktor 1000 auf.'
    },
  ]}
/>

<Quellen
  quellen={[
    {
      label: 'BIPM — SI prefixes',
      href: 'https://www.bipm.org/en/measurement-units/si-prefixes',
      detail: 'Offizielle Definition der SI-Präfixe für die dezimalen Vielfachen von Byte.'
    },
    {
      label: 'NIST — Prefixes for binary multiples',
      href: 'https://physics.nist.gov/cuu/Units/binary.html',
      detail: 'Offizielle Übersicht zu KiB, MiB, GiB und der Abgrenzung zu MB und GB.'
    },
    {
      label: 'Fachinformatikerausbildungsverordnung (FIAusbV)',
      href: 'https://www.gesetze-im-internet.de/fiausbv/BJNR025000020.html',
      detail: 'Amtliche Ausbildungsordnung als Rahmen für IT-Systeme, Daten und Vernetzung in der Ausbildung.'
    },
    {
      label: 'BIBB — Fachinformatiker/Fachinformatikerin, Umsetzungshilfe',
      href: 'https://www.bibb.de/dienst/publikationen/de/16661',
      detail: 'Ausbildungsordnung, Ausbildungsrahmenplan und Lernfelder für die modernisierten IT-Berufe.'
    },
  ]}
/>
